id = 2
slug = "volume-verbs-and-a-scrubbed-clone"
title = "Volume verbs, and a clone that does not carry an identity"
status = "plan"
created = "2026-08-17"
updated = "2026-09-15"
tags = ["area:cli"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "host/cli.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "host/services.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "capsules.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "host/volume*.nix"
intent = "scope-relevant"

[[selector]]
selector = "host/*-cases.nix"
intent = "scope-relevant"

[[selector]]
selector = "flake.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "justfile"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "host/volume-root.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "host/volume-root-cases.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "vm/reset-home.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "vm/reset-home-cases.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "vm/capsule.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "host/volume-cases.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "host/programs.nix"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "docs/contract-assignment.md"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "docs/probes.md"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[selector]]
selector = "CLAUDE.md"
intent = "design-target"
note = "SL-002 design sec-8 code impact"

[[relation]]
label = "governed_by"
target = "POL-003"

[[relation]]
label = "related"
target = "IMP-001"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
